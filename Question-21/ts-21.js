// Define an array of numbers
var numbers = [1, 2, 3, 4, 5];
// Try to access an element at an index that is out of bounds
console.log(numbers[10]); // This will produce an index error since the array has only 5 elements (indices 0 to 4)
